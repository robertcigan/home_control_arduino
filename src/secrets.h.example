#ifndef SECRETS_H
#define SECRETS_H

// OTA Update credentials (empty password "" = no authentication required)
// Copy this file to secrets.h and set your credentials
#define OTA_UPDATE_USERNAME "admin"
#define OTA_UPDATE_PASSWORD "your_password_here"

#endif

